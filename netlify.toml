[dev]
  functions = "netlify/functions/"
  functionsPort = 9000
[build]
  command = "npm run build"
  functions = "lambda"
  publish = "build"
  node_bundler = "esbuild"

[functions]
  directory = "netlify/functions"
